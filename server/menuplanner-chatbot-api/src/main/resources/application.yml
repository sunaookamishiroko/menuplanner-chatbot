spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: rootroot
    url: jdbc:mysql://127.0.0.1:3306/test2?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    hikari:
      connection-timeout: 3500

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true

# jdbc:mysql://127.0.0.1:3306/test?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul

parsing-endpoint:

parsing-message: 지금은 서버가 메뉴 정보를 얻고 있습니다. 잠시 후에 다시 시도해주세요. 메뉴 업로드가 늦으면 파싱도 늦을 수 있습니다!
monday-message: 오전 7시부터 파싱이 시작됩니다. 그 후에 찾아주세요!