spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: rootroot
    url: jdbc:mysql://127.0.0.1:3306/test?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    hikari:
      connection-timeout: 3500

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true


parsing-endpoint: 

parsing-message: 지금은 서버가 파싱중이에요 파싱이 완료될 때까지 기다려주세요. 메뉴 업로드가 늦으면 파싱도 늦을 수 있습니다!
monday-message: 오전 7시부터 파싱이 시작됩니다. 그 후에 찾아주세요!
